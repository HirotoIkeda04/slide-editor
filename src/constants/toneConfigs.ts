import type { Tone } from '../types'

export const toneConfigs: Record<Tone, { name: string }> = {
  simple: { name: 'シンプル' },
  warm: { name: '暖かい' },
  casual: { name: 'カジュアル' },
  luxury: { name: 'ラグジュアリー' }
}

